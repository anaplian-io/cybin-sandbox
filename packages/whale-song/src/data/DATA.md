# Data

This directory holds individual data files representing in game objects and entities. These files should all be strongly typed TypeScript files but should not contain substantive logic. Create subdirectories as needed.
